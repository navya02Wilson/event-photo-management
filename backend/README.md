# Backend API

Express.js backend for event photo management.

## Setup

1. Install dependencies: `npm install`
2. Copy `.env.example` to `.env` and configure
3. Run migrations
4. Start server: `npm run dev`
